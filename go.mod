module github.com/YOURNAME/vpnguard

go 1.22

// Dependencies (github.com/tailscale/wf, golang.org/x/sys, gopkg.in/yaml.v3)
// are added automatically: run `go mod tidy` once (CI does this too),
// then commit go.mod + go.sum to pin versions.
